/// A filename or URI filename specifying the duckdb database. 
/// Accepts plain paths or file: URIs (e.g., file:test.db,
/// file:///absolute/path.db). Takes precedence over path if both are set.
uri: []const u8,

